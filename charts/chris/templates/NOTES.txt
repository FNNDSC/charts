ChRIS Research Integration Service

      sSSs   .S    S.    .S_sSSs     .S    sSSs
     d%%SP  .SS    SS.  .SS~YS%%b   .SS   d%%SP
    d%S'    S%S    S%S  S%S   `S%b  S%S  d%S'
    S%S     S%S    S%S  S%S    S%S  S%S  S%|
    S&S     S%S SSSS%S  S%S    d* S  S&S  S&S
    S&S     S&S  SSS&S  S&S   .S* S  S&S  Y&Ss
    S&S     S&S    S&S  S&S_sdSSS   S&S  `S&&S
    S&S     S&S    S&S  S&S~YSY%b   S&S    `S*S
    S*b     S*S    S*S  S*S   `S%b  S*S     l*S
    S*S.    S*S    S*S  S*S    S%S  S*S    .S*P
     SSSbs  S*S    S*S  S*S    S&S  S*S  sSS*S
      YSSP  SSS    S*S  S*S    SSS  S*S  YSS'
                   SP   SP          SP
                   Y    Y           Y
 
                 U  L  T  R  O  N
{{ $kubectl := ternary "oc" "kubectl" (.Capabilities.APIVersions.Has "security.openshift.io/v1/SecurityContextConstraints") }}
{{- if .Values.migrate.enabled }}
{{- $cmd := printf "%s -n %s logs --follow job/%s" $kubectl .Release.Namespace (include "chris.migrate-job" .) }}
+----------------------------------+
| DATABASE MIGRATIONS ARE RUNNING. |
| You should monitor its logs:     |
+----------------------------------+--{{ repeat (int (sub (len $cmd) 36)) "-" }}--+
|  {{ repeat (len $cmd) " "                                                    }} |
|  {{  $cmd                                                                    }} |
|  {{ repeat (len $cmd) " "                                                    }} |
+--{{ repeat (len $cmd) "-"                                                    }}-+
{{- end }}

The ChRIS backend is being deployed. Please wait for it to be ready.
You can run this command to block while the server is starting up:

    {{ $kubectl }} -n {{ .Release.Namespace }} rollout status deploy/{{ .Release.Name }}-server

After that, try logging in as the admin user. The password can be revealed by running the command

    {{ $kubectl }} -n {{ .Release.Namespace }} get secret {{ .Release.Name }} -o jsonpath='{.data.CHRIS_SUPERUSER_PASSWORD}' | base64 --decode

{{ if .Values.cube.server.service.nodePort }}
{{- $host := print $kubectl " get nodes -o jsonpath='{.items[0].status.addresses[0].address}'" }}
Your CUBE API address is:

    http://{{ $host }}:{{ .Values.cube.server.service.nodePort }}/api/v1/

Here is a one-liner for you to check that everything is up and running:

    curl -i --fail-with-body -u "chris:$(kubectl get secret -n {{ $host }}:{{ .Values.cube.server.service.nodePort }}/chris-admin/api/v1/

{{ end }}
